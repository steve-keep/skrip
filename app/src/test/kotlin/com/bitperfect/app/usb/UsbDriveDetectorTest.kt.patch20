--- app/src/test/kotlin/com/bitperfect/app/usb/UsbDriveDetectorTest.kt
+++ app/src/test/kotlin/com/bitperfect/app/usb/UsbDriveDetectorTest.kt
@@ -93,7 +93,7 @@
                     b.putInt(0x53425355)
                     System.arraycopy(csw, 0, buffer, 0, csw.size.coerceAtMost(length))
                     currentTurAttempt++
-                    state = "TUR_CBW"
+                    state = "DONE"
                     return 13
                 }
                 "TOC_CBW" -> {
@@ -522,7 +522,7 @@
         // Let state update
         var attempts = 0
         while (detector.driveStatus.value !is DriveStatus.DiscReady && attempts < 100) {
-            fakeTransport.state = "TUR_CBW" // The problem is that testExecuteTestUnitReadyExhaustsRetries needs DONE to stop returning sense data, but interrogate test needs to continue TUR
+            if (fakeTransport.state == "DONE") fakeTransport.state = "TUR_CBW"
             Thread.sleep(50)
             org.robolectric.shadows.ShadowLooper.idleMainLooper()
             org.robolectric.shadows.ShadowLooper.runUiThreadTasksIncludingDelayedTasks()
